// This file only exists so that `src-index-file.tsx` can be accurately
//   type-checked during development.
// It is excluded from the production build by the `.npmignore` file.

export default Object.create(null);
